# Dependencias (Render/Linux debe compilarlas con npm install)
backend/node_modules/

# Secretos
backend/.env
.env
.env.*

# Documento de entrega con credenciales (no publicar)
ENTREGA.md

# Subidas del admin (se generan en el servidor)
backend/uploads/*
!backend/uploads/.gitkeep

# SQLite temporales (no commitear)
database/*.db-shm
database/*.db-wal

# OS / editor
.DS_Store
Thumbs.db
*.log
